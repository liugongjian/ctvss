const math = {
  atan2: Math.atan2,
  cos: Math.cos,
  sin: Math.sin,
  pi: Math.PI,
  pow: Math.pow,
  sqrt: Math.sqrt,
  divide: (x: number, y: number) => x / y,
  subtract: (x: number, y: number) => x - y
}
export default math
